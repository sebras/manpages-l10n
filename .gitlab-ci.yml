image: debian:latest

pages:
  stage: deploy
  script:
  - apt-get install -y --no-install-recommends gettext
  - cd public
  - ./generate-stats-page.sh > index.html
  artifacts:
    paths:
    - public
  only:
  - master
